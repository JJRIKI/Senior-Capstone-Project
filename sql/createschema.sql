CREATE TABLE Events (
	id varchar(80),
	title varchar(80),
	start varchar(80), 
	end varchar(80),
	PRIMARY KEY (id)

);

CREATE TABLE Backlog (
	id varchar(80),
	title varchar(80)

);
SHOW TABLES;
USE nimble;
